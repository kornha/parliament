import 'package:flutter/material.dart';

class ZWrapper extends StatelessWidget {
  const ZWrapper({super.key, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
